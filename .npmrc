# Configure npm to rebuild binaries
rebuild-bundle=true

# Disable optional dependencies that might cause issues
optional=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Configure binary rebuild
target_platform=linux
target_arch=x64
target_libc=glibc 